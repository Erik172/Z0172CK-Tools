{
    "configurations": [
    {
        "name": "Launch Chrome",
        "request": "launch",
        "type": "chrome",
        "url": "http://localhost/index.html",
        "webRoot": "${workspaceFolder}"
    }
    ]
}